import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#5CB646",
      contrastText: "#fff",
    },
    text: {
      primary: "#000",
    },
  },
});

export default theme;
